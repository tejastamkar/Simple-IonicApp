import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.cardappwithionic',
  appName: 'cardappwithionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
